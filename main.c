#include <stdio.h>
#include <stdbool.h>

int main() {

    bool isSunny = false;

    if(isSunny) {
        printf("It is sunny outside");
    } else {
        printf("It is cloudy outside");
    }


    return 0;
}